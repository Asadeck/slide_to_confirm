import 'package:flutter_test/flutter_test.dart';

import 'package:slide_to_confirm/slide_to_confirm.dart';

void main() {
  test('Given int and decimal places When converted Then returns type double',
      () {
    // Assert
    expect(double, double,
        reason: 'the method should return a value of type double');
  });
}
